platform: linux

image_resource:
  type: docker-image
  source:
    repository: node

inputs:
  - name: source-code

outputs:
  - name: build-output

run:
  path: source-code/concourse/frontend/build-dev/run.sh